namespace Glint.Core.Logging;

/// <summary>
/// Severity levels for application logging. DEBUG is opt-in via settings.
/// </summary>
public enum LogLevel
{
    Debug = 0,
    Info = 1,
    Warn = 2,
    Error = 3
}
